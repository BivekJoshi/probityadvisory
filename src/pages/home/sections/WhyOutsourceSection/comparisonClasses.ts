/* shared by the header row and every body row, so the columns line up */
export const columns = 'sm:grid-cols-[minmax(0,1.1fr)_minmax(0,1fr)_minmax(0,1fr)]'
export const smallCaps = 'text-[11.5px] font-semibold uppercase tracking-[0.12em]'
